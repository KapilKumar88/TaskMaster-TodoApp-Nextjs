export enum TaskListTypes {
  ALL = 'all',
  TODAY = 'today',
  UPCOMING = 'upcoming',
  COMPLETED = 'completed',
}

export enum ToastVariation {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
  WARNING = 'warning',
  ACTION = 'action',
  DEFAULT = 'default',
}
