export enum TaskListTypes {
  ALL = 'all',
  TODAY = 'today',
  UPCOMING = 'upcoming',
  COMPLETED = 'completed',
}

export enum ToastVariation {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
  WARNING = 'warning',
  ACTION = 'action',
  DEFAULT = 'default',
  LINK = 'link',
}

export enum DirectionIndicators {
  UP,
  DOWN,
}

export enum EMAIL_NOTIFICATION_TEMPLATE {
  TASK_DUE = 'task-due',
  TASK_EXPIRED = 'task-expired',
  TASK_COMPLETED = 'task-completed',
  TASK_CREATED = 'task-created',
  TASK_UPDATED = 'task-updated',
}
